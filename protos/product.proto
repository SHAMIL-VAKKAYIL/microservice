syntax ="proto3";

package product;

service ProductService{
    rpc productsById(getProduct) returns(products);
}

message getProduct{
   string id=1;
}

message products{
    string id=1;
    string name=2;
    double price=3;
}